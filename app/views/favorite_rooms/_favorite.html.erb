<% if current_user.admin? %>
<% else %>
  <% if room.favorited_by?(current_user) %>
    <%= link_to 'お気に入り解除', room_favorite_rooms_path(room.id), method: :delete, class: "btn btn-primary" %>
  <% else %>
    <%= link_to 'お気に入り', room_favorite_rooms_path(room.id), method: :post, class: "btn btn-primary" %>
  <% end %>
  
  <%= link_to 'お気に入りユーザー一覧', members_room_path(room.id), class: "btn btn-success" %>
<% end %>
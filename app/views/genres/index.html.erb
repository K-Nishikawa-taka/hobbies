<h1>ジャンル一覧</h1>

<%= render 'error', genre: @genre %>

<%= render 'new', genre: @genre %>

<table>
  <thead>
    <th>ジャンル名</th>
    <th></th>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre_path(genre.id) do %>
            <%= genre.name %>
          <% end %>
        </td>
        <td>
          <%= link_to '参加者一覧', members_genre_path(genre.id) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

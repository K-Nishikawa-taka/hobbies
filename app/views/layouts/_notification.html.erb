<% is_break = false %>
<% current_user.messages.each do |message| %>
  <% message.comments.each do |comment| %>
    <% if comment.is_read == false %>
      <%= link_to new_comments_path do %>
        <p>未読あり</p>
      <% end %>
      <% is_break = true %>
      <% break %>
    <% end %>
    <% break if is_break == true %>
  <% end %>
  <% break if is_break == true %>
<% end %>

<% if current_user.admin? %>
  ♡<%= message.favorite_messages.count %>
<% else %>
  <% if message.favorited_by?(current_user) %>
    <%= link_to "♥ #{message.favorite_messages.count}", room_message_favorite_messages_path(message.room.id, message.id), method: :delete %>
  <% else %>
    <%= link_to "♡ #{message.favorite_messages.count}", room_message_favorite_messages_path(message.room.id, message.id), method: :post %>
  <% end %>
<% end %>
<%= link_to 'お気に入りユーザー一覧', favorite_users_room_message_path(message.room.id, message.id) %>